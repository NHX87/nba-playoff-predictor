# Copy to .streamlit/secrets.toml locally (never commit secrets.toml)
# On Streamlit Community Cloud, set these under App Settings → Secrets

DB_PATH = "data/processed/nba_app.duckdb"
ANTHROPIC_API_KEY = "sk-ant-..."
